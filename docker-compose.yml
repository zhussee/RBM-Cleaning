networks:
  dev:

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
    volumes:
      - './nginx.conf:/etc/nginx/nginx.conf'
      - './frontend/dist:/usr/share/nginx/html'
    networks:
      - dev
    depends_on:
      - backend

